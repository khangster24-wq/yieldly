import { PageTransition } from "@/components/layout/page-transition";
import { AdvisorExperience } from "@/components/advisor/advisor-experience";

export default function AdvisorPage() {
  return (
    <PageTransition>
      <AdvisorExperience />
    </PageTransition>
  );
}
